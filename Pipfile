[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
'cherrypy' = '>=18.4.0'
'pillow' = '>=6.2.1'
'python-magic' = '>=0.4.15'
'pyyaml' = '>=5.1.2'
'requests' = '>=2.22.0'

[dev-packages]
'nose' = '==1.3.7' # top, latest
'pytest-cov' = '==2.8.1' # latest
    'coverage' = '==4.5.4' # latest
    'pytest' = '>=4.1.1, <4.2.0' # NOT LATEST (5.3.1)
                                 # TODO: First handler integration test of >=2nd
                                 #  handler suite always fails. Not sure if this
                                 #  is PyTest or CherryPy's problem.
        'attrs' = '==19.1.0, <19.2.0' # NOT LATEST (19.3.0).
                                      # See: https://github.com/pytest-dev/pytest/issues/5903


[requires]
python_version = "3.7"

# Specifying Versions:
#   https://pipenv.readthedocs.io/en/latest/basics/#specifying-versions-of-a-package
